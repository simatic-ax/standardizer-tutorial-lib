name: standardizer-tutorial-lib
version: 0.0.1
type: lib

targets:
  - "axunit-llvm"

devDependencies:
  "@ax/sdk": 0.2.449

variables:
  APAX_BUILD_ARGS:
    - --debug
scripts:
  prebuild:
    - rm $TRACE_FILE -f
dependencies:
  "@simatic-ax/io": 0.0.2
  "@simatic-ax/collections": 0.0.1
files:
  - "./src"
  - "./doc"
registries:
  '@simatic-ax': 'https://npm.pkg.github.com/'
